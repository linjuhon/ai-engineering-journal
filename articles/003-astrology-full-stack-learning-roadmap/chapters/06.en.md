[← Back to technical index](../TECHNICAL_INDEX.en.md) | **English** | [繁體中文](06.md)

---

# 06 | Python

**Role in this Roadmap**: Core for celestial computation, AI, numerical analysis, and data processing.

### 01. Python execution model, interpreter, bytecode, and environment

**Core Question**: This chapter is not about memorizing APIs. It explains why "Python execution model, interpreter, bytecode, and environment" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 02. Virtual environment, package management, lock files, and reproducible installation

**Core Question**: This chapter is not about memorizing APIs. It explains why "Virtual environment, package management, lock files, and reproducible installation" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 03. Basic types, containers, and mutability/immutability

**Core Question**: This chapter is not about memorizing APIs. It explains why "Basic types, containers, and mutability/immutability" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 04. Functions, parameters, closures, and decorators

**Core Question**: This chapter is not about memorizing APIs. It explains why "Functions, parameters, closures, and decorators" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 05. class, dataclass, protocol, and abstraction

**Core Question**: This chapter is not about memorizing APIs. It explains why "class, dataclass, protocol, and abstraction" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 06. Type hints, mypy mindset, and runtime limitations

**Core Question**: This chapter is not about memorizing APIs. It explains why "Type hints, mypy mindset, and runtime limitations" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 07. module, package, \_\_init\_\_, and import graph

**Core Question**: This chapter is not about memorizing APIs. It explains why "module, package, \_\_init\_\_, and import graph" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 08. Exception hierarchy and error contracts

**Core Question**: This chapter is not about memorizing APIs. It explains why "Exception hierarchy and error contracts" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 09. Context manager and resource release

**Core Question**: This chapter is not about memorizing APIs. It explains why "Context manager and resource release" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 10. iterator, generator, and streaming data

**Core Question**: This chapter is not about memorizing APIs. It explains why "iterator, generator, and streaming data" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 11. asyncio, coroutine, task, and event loop

**Core Question**: This chapter is not about memorizing APIs. It explains why "asyncio, coroutine, task, and event loop" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 12. thread, process, GIL, and CPU-intensive work

**Core Question**: This chapter is not about memorizing APIs. It explains why "thread, process, GIL, and CPU-intensive work" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 13. NumPy vectorization vs. pure Python loops

**Core Question**: This chapter is not about memorizing APIs. It explains why "NumPy vectorization vs. pure Python loops" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 14. logging, configuration, and structured logging

**Core Question**: This chapter is not about memorizing APIs. It explains why "logging, configuration, and structured logging" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 15. Testing, fixture, mock, and property testing

**Core Question**: This chapter is not about memorizing APIs. It explains why "Testing, fixture, mock, and property testing" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 16. Performance profiling, memory, and numerical stability

**Core Question**: This chapter is not about memorizing APIs. It explains why "Performance profiling, memory, and numerical stability" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 17. Data validation, serialization, and API DTO

**Core Question**: This chapter is not about memorizing APIs. It explains why "Data validation, serialization, and API DTO" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 18. Python service layering and dependency injection

**Core Question**: This chapter is not about memorizing APIs. It explains why "Python service layering and dependency injection" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 19. Aligning with Node contracts and version management

**Core Question**: This chapter is not about memorizing APIs. It explains why "Aligning with Node contracts and version management" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 20. Capstone: building the domain core of a computation service

**Core Question**: This chapter is not about memorizing APIs. It explains why "Capstone: building the domain core of a computation service" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 06 | Python → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Core for celestial computation, AI, numerical analysis, and data processing.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.
