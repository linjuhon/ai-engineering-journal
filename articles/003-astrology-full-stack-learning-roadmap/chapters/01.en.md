[← Back to technical index](../TECHNICAL_INDEX.en.md) | **English** | [繁體中文](01.md)

---

# 01 | Tree-Structured Codebase and Monorepo Architecture

**Role in this Roadmap**: Determine the single home for all React, Node, Python, data, deployment, and test files.

### 01. Why Design the Dependency Tree Before Creating Folders

**Core Question**: This chapter is not about memorizing APIs. It explains why "Why Design the Dependency Tree Before Creating Folders" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 02. The Six Layers: Domain, Application, Services, Packages, Infrastructure, and Documentation

**Core Question**: This chapter is not about memorizing APIs. It explains why "The Six Layers: Domain, Application, Services, Packages, Infrastructure, and Documentation" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 03. The Responsibilities of apps, services, packages, infra, tests, and docs

**Core Question**: This chapter is not about memorizing APIs. It explains why "The Responsibilities of apps, services, packages, infra, tests, and docs" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 04. Pros and Cons of Splitting by Technology vs. by Business Capability

**Core Question**: This chapter is not about memorizing APIs. It explains why "Pros and Cons of Splitting by Technology vs. by Business Capability" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 05. How Frontend Feature-Sliced Architecture Aligns with Backend Vertical Slices

**Core Question**: This chapter is not about memorizing APIs. It explains why "How Frontend Feature-Sliced Architecture Aligns with Backend Vertical Slices" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 06. How a Shared contracts Package Becomes the Single Source of Truth for Frontend and Backend

**Core Question**: This chapter is not about memorizing APIs. It explains why "How a Shared contracts Package Becomes the Single Source of Truth for Frontend and Backend" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 07. The Four Layers: Domain, Application, Adapters, Infrastructure

**Core Question**: This chapter is not about memorizing APIs. It explains why "The Four Layers: Domain, Application, Adapters, Infrastructure" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 08. Dependency Rules Prohibiting Cross-Layer Imports

**Core Question**: This chapter is not about memorizing APIs. It explains why "Dependency Rules Prohibiting Cross-Layer Imports" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 09. Directory Naming, File Naming, Export Entry Points, and Barrel Files

**Core Question**: This chapter is not about memorizing APIs. It explains why "Directory Naming, File Naming, Export Entry Points, and Barrel Files" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 10. Placement of Configuration Files, Environment Variables, Secrets, and Example Settings

**Core Question**: This chapter is not about memorizing APIs. It explains why "Placement of Configuration Files, Environment Variables, Secrets, and Example Settings" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 11. Placement of Database Migrations, Seeds, Fixtures, and Schemas

**Core Question**: This chapter is not about memorizing APIs. It explains why "Placement of Database Migrations, Seeds, Fixtures, and Schemas" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 12. Two Strategies: Mirrored Test Directory vs. Co-located Tests

**Core Question**: This chapter is not about memorizing APIs. It explains why "Two Strategies: Mirrored Test Directory vs. Co-located Tests" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 13. How API, Events, Tables, and Documentation Versions Co-evolve

**Core Question**: This chapter is not about memorizing APIs. It explains why "How API, Events, Tables, and Documentation Versions Co-evolve" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 14. Isolation of Generated Code and Handwritten Code

**Core Question**: This chapter is not about memorizing APIs. It explains why "Isolation of Generated Code and Handwritten Code" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 15. Isolation of Large Data, Models, Ephemeris Files, and Code

**Core Question**: This chapter is not about memorizing APIs. It explains why "Isolation of Large Data, Models, Ephemeris Files, and Code" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 16. Isolation of Scripts, One-Time Tools, and the Production Runtime

**Core Question**: This chapter is not about memorizing APIs. It explains why "Isolation of Scripts, One-Time Tools, and the Production Runtime" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 17. Ownership, CODEOWNERS, Module README, and ADR

**Core Question**: This chapter is not about memorizing APIs. It explains why "Ownership, CODEOWNERS, Module README, and ADR" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 18. Circular Dependency Checks, Unused Files, and Boundary Validation

**Core Question**: This chapter is not about memorizing APIs. It explains why "Circular Dependency Checks, Unused Files, and Boundary Validation" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 19. Building a Bootable Skeleton Repository from an Empty Directory

**Core Question**: This chapter is not about memorizing APIs. It explains why "Building a Bootable Skeleton Repository from an Empty Directory" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 20. Final Exercise: Explain Why Each Folder Exists Using a Tree Diagram

**Core Question**: This chapter is not about memorizing APIs. It explains why "Final Exercise: Explain Why Each Folder Exists Using a Tree Diagram" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 01 | Tree-Structured Codebase and Monorepo Architecture → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Determine the single home for all React, Node, Python, data, deployment, and test files.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.
