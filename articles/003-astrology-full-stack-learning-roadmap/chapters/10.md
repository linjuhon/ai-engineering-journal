[← 回到本研究總綱](../README.md)

---

# 10｜Swiss Ephemeris

**本軌角色**：行星、宮位、交點、逆行與星曆計算核心。


### 01. 星曆表、天體位置與 Swiss Ephemeris 的角色

**核心問題**：這一章不是只背 API，而是回答「星曆表、天體位置與 Swiss Ephemeris 的角色」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 02. 安裝、資料檔、授權與部署考量

**核心問題**：這一章不是只背 API，而是回答「安裝、資料檔、授權與部署考量」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 03. 儒略日、UTC、UT1、TT 的輸入

**核心問題**：這一章不是只背 API，而是回答「儒略日、UTC、UT1、TT 的輸入」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 04. 行星代碼、flags 與回傳值

**核心問題**：這一章不是只背 API，而是回答「行星代碼、flags 與回傳值」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 05. 地心、日心、真位置、視位置的選擇

**核心問題**：這一章不是只背 API，而是回答「地心、日心、真位置、視位置的選擇」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 06. 黃經、黃緯、距離與速度

**核心問題**：這一章不是只背 API，而是回答「黃經、黃緯、距離與速度」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 07. 逆行與停滯如何從速度判定

**核心問題**：這一章不是只背 API，而是回答「逆行與停滯如何從速度判定」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 08. 月交點、Lilith 與不同定義

**核心問題**：這一章不是只背 API，而是回答「月交點、Lilith 與不同定義」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 09. 小行星與外部星曆檔

**核心問題**：這一章不是只背 API，而是回答「小行星與外部星曆檔」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 10. houses、house cusps、ASC、MC

**核心問題**：這一章不是只背 API，而是回答「houses、house cusps、ASC、MC」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 11. Placidus、Koch、Equal、Whole Sign

**核心問題**：這一章不是只背 API，而是回答「Placidus、Koch、Equal、Whole Sign」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 12. 高緯度宮位失敗與降級策略

**核心問題**：這一章不是只背 API，而是回答「高緯度宮位失敗與降級策略」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 13. 固定星與特殊點

**核心問題**：這一章不是只背 API，而是回答「固定星與特殊點」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 14. 日食月食與事件搜尋

**核心問題**：這一章不是只背 API，而是回答「日食月食與事件搜尋」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 15. 批次計算、快取與性能

**核心問題**：這一章不是只背 API，而是回答「批次計算、快取與性能」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 16. 執行緒安全、全域設定與程序隔離

**核心問題**：這一章不是只背 API，而是回答「執行緒安全、全域設定與程序隔離」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 17. 錯誤碼、缺檔與回退

**核心問題**：這一章不是只背 API，而是回答「錯誤碼、缺檔與回退」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 18. 與 Astropy 交叉驗證

**核心問題**：這一章不是只背 API，而是回答「與 Astropy 交叉驗證」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 19. 建立純 domain 的 EphemerisPort

**核心問題**：這一章不是只背 API，而是回答「建立純 domain 的 EphemerisPort」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 20. 總作業：產出可重跑且可驗證的完整星盤

**核心問題**：這一章不是只背 API，而是回答「總作業：產出可重跑且可驗證的完整星盤」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 10｜Swiss Ephemeris → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：行星、宮位、交點、逆行與星曆計算核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。
