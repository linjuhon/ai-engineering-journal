[← Back to technical index](../TECHNICAL_INDEX.en.md) | **English** | [繁體中文](15.md)

---

# 15 | Ant Design

**Role in this Roadmap**: Consistent, usable, accessible frontend component system.


### 01. Design System, Token, Theme, and Consistency

**Core Question**: This chapter is not about memorizing APIs. It explains why "Design System, Token, Theme, and Consistency" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 02. Layout, Grid, and Responsive Pages

**Core Question**: This chapter is not about memorizing APIs. It explains why "Layout, Grid, and Responsive Pages" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 03. Form, Form.Item, and Validation

**Core Question**: This chapter is not about memorizing APIs. It explains why "Form, Form.Item, and Validation" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 04. Input, DatePicker, TimePicker

**Core Question**: This chapter is not about memorizing APIs. It explains why "Input, DatePicker, TimePicker" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 05. Select, AutoComplete, and Remote Search

**Core Question**: This chapter is not about memorizing APIs. It explains why "Select, AutoComplete, and Remote Search" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 06. Table, Pagination, and Server Data

**Core Question**: This chapter is not about memorizing APIs. It explains why "Table, Pagination, and Server Data" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 07. Modal, Drawer, Popover Interaction Boundaries

**Core Question**: This chapter is not about memorizing APIs. It explains why "Modal, Drawer, Popover Interaction Boundaries" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 08. Steps, Progress, and Nine-Stage Progress

**Core Question**: This chapter is not about memorizing APIs. It explains why "Steps, Progress, and Nine-Stage Progress" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 09. Alert, Result, Empty, and Error States

**Core Question**: This chapter is not about memorizing APIs. It explains why "Alert, Result, Empty, and Error States" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 10. Spin, Skeleton, and Loading Experience

**Core Question**: This chapter is not about memorizing APIs. It explains why "Spin, Skeleton, and Loading Experience" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 11. Notification, Message, and Feedback Levels

**Core Question**: This chapter is not about memorizing APIs. It explains why "Notification, Message, and Feedback Levels" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 12. Upload and File Validation

**Core Question**: This chapter is not about memorizing APIs. It explains why "Upload and File Validation" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 13. Tree, TreeSelect, and Knowledge Tree

**Core Question**: This chapter is not about memorizing APIs. It explains why "Tree, TreeSelect, and Knowledge Tree" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 14. Tabs, Collapse, and Long Reports

**Core Question**: This chapter is not about memorizing APIs. It explains why "Tabs, Collapse, and Long Reports" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 15. Accessibility and Keyboard Navigation

**Core Question**: This chapter is not about memorizing APIs. It explains why "Accessibility and Keyboard Navigation" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 16. Theme Override and Branding

**Core Question**: This chapter is not about memorizing APIs. It explains why "Theme Override and Branding" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 17. Large Form Performance

**Core Question**: This chapter is not about memorizing APIs. It explains why "Large Form Performance" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 18. Trade-offs with React Hook Form / Native Form

**Core Question**: This chapter is not about memorizing APIs. It explains why "Trade-offs with React Hook Form / Native Form" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 19. Building an Astrology Domain Component Library

**Core Question**: This chapter is not about memorizing APIs. It explains why "Building an Astrology Domain Component Library" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 20. Final Exercise: Usable, Testable, Accessible Complete Interface

**Core Question**: This chapter is not about memorizing APIs. It explains why "Final Exercise: Usable, Testable, Accessible Complete Interface" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 15 | Ant Design → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Consistent, usable, accessible frontend component system.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.
