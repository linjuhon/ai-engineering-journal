[← 回到本研究總綱](../README.md)

---

# 06｜Python

**本軌角色**：天體計算、AI、數值分析與資料處理核心。


### 01. Python 執行模型、直譯器、bytecode 與環境

**核心問題**：這一章不是只背 API，而是回答「Python 執行模型、直譯器、bytecode 與環境」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 02. 虛擬環境、套件管理、鎖檔與可重現安裝

**核心問題**：這一章不是只背 API，而是回答「虛擬環境、套件管理、鎖檔與可重現安裝」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 03. 基本型別、容器與可變／不可變

**核心問題**：這一章不是只背 API，而是回答「基本型別、容器與可變／不可變」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 04. 函式、參數、closure、decorator

**核心問題**：這一章不是只背 API，而是回答「函式、參數、closure、decorator」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 05. class、dataclass、protocol 與抽象

**核心問題**：這一章不是只背 API，而是回答「class、dataclass、protocol 與抽象」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 06. type hints、mypy 思維與 runtime 限制

**核心問題**：這一章不是只背 API，而是回答「type hints、mypy 思維與 runtime 限制」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 07. module、package、__init__ 與 import graph

**核心問題**：這一章不是只背 API，而是回答「module、package、__init__ 與 import graph」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 08. exception hierarchy 與錯誤契約

**核心問題**：這一章不是只背 API，而是回答「exception hierarchy 與錯誤契約」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 09. context manager 與資源釋放

**核心問題**：這一章不是只背 API，而是回答「context manager 與資源釋放」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 10. iterator、generator 與串流資料

**核心問題**：這一章不是只背 API，而是回答「iterator、generator 與串流資料」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 11. asyncio、coroutine、task 與 event loop

**核心問題**：這一章不是只背 API，而是回答「asyncio、coroutine、task 與 event loop」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 12. thread、process、GIL 與計算密集工作

**核心問題**：這一章不是只背 API，而是回答「thread、process、GIL 與計算密集工作」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 13. NumPy 向量化與純 Python 迴圈差異

**核心問題**：這一章不是只背 API，而是回答「NumPy 向量化與純 Python 迴圈差異」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 14. logging、設定與結構化日誌

**核心問題**：這一章不是只背 API，而是回答「logging、設定與結構化日誌」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 15. 測試、fixture、mock 與 property testing

**核心問題**：這一章不是只背 API，而是回答「測試、fixture、mock 與 property testing」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 16. 效能剖析、記憶體與數值穩定性

**核心問題**：這一章不是只背 API，而是回答「效能剖析、記憶體與數值穩定性」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 17. 資料驗證、序列化與 API DTO

**核心問題**：這一章不是只背 API，而是回答「資料驗證、序列化與 API DTO」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 18. Python 服務的分層與依賴注入

**核心問題**：這一章不是只背 API，而是回答「Python 服務的分層與依賴注入」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 19. 與 Node 契約對齊及版本管理

**核心問題**：這一章不是只背 API，而是回答「與 Node 契約對齊及版本管理」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 20. 總作業：建立計算服務的 domain 核心

**核心問題**：這一章不是只背 API，而是回答「總作業：建立計算服務的 domain 核心」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**代碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 06｜Python → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：天體計算、AI、數值分析與資料處理核心。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。
