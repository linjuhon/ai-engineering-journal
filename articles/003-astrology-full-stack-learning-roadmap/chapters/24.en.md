[← Back to technical index](../TECHNICAL_INDEX.en.md) | **English** | [繁體中文](24.md)

---

# 24 | Kubernetes

**Role in this Roadmap**: Production service deployment, scaling, isolation, and recovery.


### 01. Control Plane, Node, Pod, and Declarative State

**Core Question**: This chapter is not about memorizing APIs. It explains why "Control Plane, Node, Pod, and Declarative State" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 02. Deployment, ReplicaSet, and Rollout

**Core Question**: This chapter is not about memorizing APIs. It explains why "Deployment, ReplicaSet, and Rollout" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 03. Service, DNS, and Service Discovery

**Core Question**: This chapter is not about memorizing APIs. It explains why "Service, DNS, and Service Discovery" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 04. Ingress, Gateway, and External Traffic

**Core Question**: This chapter is not about memorizing APIs. It explains why "Ingress, Gateway, and External Traffic" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 05. ConfigMap, Secret, and Configuration

**Core Question**: This chapter is not about memorizing APIs. It explains why "ConfigMap, Secret, and Configuration" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 06. requests, limits, and Scheduling

**Core Question**: This chapter is not about memorizing APIs. It explains why "requests, limits, and Scheduling" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 07. liveness, readiness, and startup probes

**Core Question**: This chapter is not about memorizing APIs. It explains why "liveness, readiness, and startup probes" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 08. HPA and Autoscaling

**Core Question**: This chapter is not about memorizing APIs. It explains why "HPA and Autoscaling" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 09. Job, CronJob, and One-Time Tasks

**Core Question**: This chapter is not about memorizing APIs. It explains why "Job, CronJob, and One-Time Tasks" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 10. StatefulSet and Stateful Services

**Core Question**: This chapter is not about memorizing APIs. It explains why "StatefulSet and Stateful Services" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 11. PersistentVolume and Data

**Core Question**: This chapter is not about memorizing APIs. It explains why "PersistentVolume and Data" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 12. namespace, RBAC, and Isolation

**Core Question**: This chapter is not about memorizing APIs. It explains why "namespace, RBAC, and Isolation" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 13. network policy

**Core Question**: This chapter is not about memorizing APIs. It explains why "network policy" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 14. rolling, blue-green, canary

**Core Question**: This chapter is not about memorizing APIs. It explains why "rolling, blue-green, canary" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 15. Pod Disruption and High Availability

**Core Question**: This chapter is not about memorizing APIs. It explains why "Pod Disruption and High Availability" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 16. Logging, Metrics, and Tracing Injection

**Core Question**: This chapter is not about memorizing APIs. It explains why "Logging, Metrics, and Tracing Injection" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 17. Helm/Kustomize Configuration Governance

**Core Question**: This chapter is not about memorizing APIs. It explains why "Helm/Kustomize Configuration Governance" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 18. Troubleshooting: events, logs, exec

**Core Question**: This chapter is not about memorizing APIs. It explains why "Troubleshooting: events, logs, exec" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 19. Astrology Microservice Topology and Dependencies

**Core Question**: This chapter is not about memorizing APIs. It explains why "Astrology Microservice Topology and Dependencies" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.

### 20. Capstone: Rollbackable and Observable Deployment

**Core Question**: This chapter is not about memorizing APIs. It explains why "Capstone: Rollbackable and Observable Deployment" matters to the entire system, where the data comes from, and where it ultimately flows.

**Code Construction Order**
1. First write input/output contracts and error types.
2. Then write the minimal pure core logic, prohibiting direct dependencies on UI, HTTP, or databases.
3. Connect to frameworks or external software via adapters.
4. Add three sets of tests: normal, boundary, and failure.
5. Finally add logging, metrics, tracing, and deployment configuration.

**Operational and Execution Logic**: When studying, you must trace step by step: "caller → 24 | Kubernetes → downstream dependencies → return value / side effects → failure fallback", and draw at least one sequence diagram.

**Project Mapping**: Production service deployment, scaling, isolation, and recovery.

**Chapter Deliverable**: An executable minimal example, a formal project file, a set of tests, a failure experiment, and a data flow note you can explain in your own words.

**Passing Criteria**: You cannot just say "I know how to use it." You must be able to point out the file location, function entry point, data types, dependency direction, error exit points, and re-runnable commands.
