[← 回到技術索引](../TECHNICAL_INDEX.md) | [English](33.en.md) | **繁體中文**

---

# 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube

**本軌角色**：把風格、邊界、複雜度與品質門檻自動化。


### 01. 格式、風格、語意錯誤與品質債的區別

**核心問題**：這一章不是只背 API，而是回答「格式、風格、語意錯誤與品質債的區別」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 02. ESLint parser、plugin、rule

**核心問題**：這一章不是只背 API，而是回答「ESLint parser、plugin、rule」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 03. TypeScript-aware lint

**核心問題**：這一章不是只背 API，而是回答「TypeScript-aware lint」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 04. import boundary rule

**核心問題**：這一章不是只背 API，而是回答「import boundary rule」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 05. Prettier 與 lint 的責任分離

**核心問題**：這一章不是只背 API，而是回答「Prettier 與 lint 的責任分離」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 06. ignore、generated code 與例外

**核心問題**：這一章不是只背 API，而是回答「ignore、generated code 與例外」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 07. Husky 與 Git hook

**核心問題**：這一章不是只背 API，而是回答「Husky 與 Git hook」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 08. lint-staged 與只檢查變更

**核心問題**：這一章不是只背 API，而是回答「lint-staged 與只檢查變更」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 09. commit message 與變更語意

**核心問題**：這一章不是只背 API，而是回答「commit message 與變更語意」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 10. SonarQube issue、smell、bug、vulnerability

**核心問題**：這一章不是只背 API，而是回答「SonarQube issue、smell、bug、vulnerability」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 11. 複雜度與認知複雜度

**核心問題**：這一章不是只背 API，而是回答「複雜度與認知複雜度」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 12. 重複碼與合理抽象

**核心問題**：這一章不是只背 API，而是回答「重複碼與合理抽象」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 13. coverage 不是品質本身

**核心問題**：這一章不是只背 API，而是回答「coverage 不是品質本身」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 14. security hotspot

**核心問題**：這一章不是只背 API，而是回答「security hotspot」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 15. quality profile 與 quality gate

**核心問題**：這一章不是只背 API，而是回答「quality profile 與 quality gate」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 16. 前端、Node、Python 多語言治理

**核心問題**：這一章不是只背 API，而是回答「前端、Node、Python 多語言治理」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 17. CI 與本地規則一致

**核心問題**：這一章不是只背 API，而是回答「CI 與本地規則一致」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 18. 依賴邊界自動驗證

**核心問題**：這一章不是只背 API，而是回答「依賴邊界自動驗證」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 19. 零警告政策的合理邊界

**核心問題**：這一章不是只背 API，而是回答「零警告政策的合理邊界」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。

### 20. 總作業：可執行的品質契約

**核心問題**：這一章不是只背 API，而是回答「總作業：可執行的品質契約」在整個系統中為何存在、資料從哪裡來、最後流向哪裡。

**程式碼建立順序**
1. 先寫輸入／輸出契約與錯誤型別。
2. 再寫最小純核心邏輯，禁止直接依賴 UI、HTTP 或資料庫。
3. 以 adapter 接上框架或外部軟體。
4. 補正常、邊界、失敗三組測試。
5. 最後加入日誌、指標、trace 與部署設定。

**操作與執行邏輯**：學習時必須逐步追蹤「呼叫端 → 33｜程式碼品質：ESLint／Prettier／Husky／SonarQube → 下游依賴 → 回傳值／副作用 → 失敗回退」，並畫出至少一張序列圖。

**本專案對應**：把風格、邊界、複雜度與品質門檻自動化。

**本章產物**：一個可執行最小範例、一個正式專案檔案、一組測試、一段故障實驗，以及一份你能用自己的話說明的資料流筆記。

**通關標準**：不能只說「會用」；必須能指出檔案位置、函式入口、資料型別、依賴方向、錯誤出口與可重跑命令。
